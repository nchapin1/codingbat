def first_half(str):
    """Given a string of even length, return the first half.
    So the string "WooHoo" yields "Woo"."""
    first = len(str) / 2
    second = str[:first]
    return second


# Solution
# No solution available
# Note: Put var 'first' in [first:] and returned the second half.
# REMEMBER: [END:BEGINNING]