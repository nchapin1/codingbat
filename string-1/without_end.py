def without_end(str):
    """Given a string, return a version without the first
    and last char, so "Hello" yields "ell". The string length
    will be at least 2."""
    # first = str[:1]
    # last = str[-1:]
    return str[1:-1]


# Note: created variables 'first' and 'last' to test selecting character positions in str
# Note: str[1:-1] means skipping char 0 and up to the last character
# Solution
# No solution available